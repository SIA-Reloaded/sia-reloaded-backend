AWSTemplateFormatVersion: 2010-09-09
Description: suia-stack
Transform:
- AWS::Serverless-2016-10-31
Resources:
  getEvaluacionDocente:
    Type: AWS::Serverless::Function
    Properties:
      Handler: src/handlers/evaluacion-docente/get-evaluacion-docente.getEvaluacionDocenteHandler
      Runtime: nodejs12.x
      MemorySize: 128
      Timeout: 100
      Description: Gets one teacher evaluation with a HTTP get method from a DynamoDB
        table.
      Policies:
      - DynamoDBCrudPolicy:
          TableName: ProfessorData
      Environment:
        Variables:
          TABLE: ProfessorData
      Events:
        Api:
          Type: Api
          Properties:
            Path: /
            Method: GET
      CodeUri: getEvaluacionDocente
Outputs:
  WebEndpoint:
    Description: API Gateway endpoint URL for Prod stage
    Value:
      Fn::Sub: https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/
